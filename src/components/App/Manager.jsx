/* eslint-disable react/prop-types */
import React from 'react';

/*
  This component should only manage the state and the logic
*/

const Manager = ({ children }) => {
  return <div>{children}</div>;
};

export default Manager;
